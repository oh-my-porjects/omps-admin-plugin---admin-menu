{
  "method": "DELETE",
  "path": "/api/admin-meta/specs/{module}",
  "endpoint_key": "DELETE /api/admin-meta/specs/{module}",
  "title": "删除模块后台规格（兼容路径）",
  "description": "admin-menu 模块保留的 /api/admin-meta 兼容路径，与 /api/admin-menu 对应接口共享同一处理逻辑。",
  "auth": "admin",
  "params": [
    {
      "name": "module",
      "type": "string",
      "required": true,
      "description": "模块名路径参数。"
    }
  ],
  "response": {
    "data_type": "object",
    "fields": [
      {
        "name": "status",
        "type": "number",
        "description": "业务状态码；0 表示成功，非 0 表示失败。",
        "enum_values": [
          {
            "value": 0,
            "label": "成功"
          },
          {
            "value": 1,
            "label": "失败或未注册"
          }
        ]
      },
      {
        "name": "msg",
        "type": "string",
        "description": "业务处理结果说明或错误原因。"
      },
      {
        "name": "data",
        "type": "object",
        "description": "接口返回数据；无结果时为空对象。",
        "fields": []
      }
    ]
  },
  "rules": [
    {
      "category": "business",
      "title": "稳定契约",
      "body": "admin-menu 模块保留的 /api/admin-meta 兼容路径，与 /api/admin-menu 对应接口共享同一处理逻辑。",
      "code_loc": "main.go:Routes"
    }
  ]
}
